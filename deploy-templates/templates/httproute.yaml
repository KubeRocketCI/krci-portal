{{- /* Opt-in (default off). Enable only on clusters that serve the Gateway API; if the
CRDs are absent the resource fails at apply time with a clear "no matches for kind
HTTPRoute" error. */ -}}
{{- if .Values.httproute.enabled -}}
{{- $fullName := include "krci-portal.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
{{- $dnsWildcard := coalesce .Values.httproute.dnsWildcard .Values.ingress.dnsWildcard "" -}}
{{- if not .Values.httproute.parentRefs -}}
{{- fail "httproute.parentRefs must contain at least one Gateway reference" -}}
{{- end -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "krci-portal.labels" . | nindent 4 }}
  {{- with .Values.httproute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- toYaml .Values.httproute.parentRefs | nindent 4 }}
  {{- if .Values.httproute.hostnames }}
  hostnames:
    {{- range .Values.httproute.hostnames }}
    {{- /* 'edpDefault' is a sentinel: renders <fullname>-<namespace>.<dnsWildcard> — the
    same host the Ingress template renders for its 'edpDefault', so the HTTPRoute takes
    over exactly the host the Ingress serves; otherwise the literal hostname is used as-is */ -}}
    {{- if eq . "edpDefault" }}
    {{- if not $dnsWildcard }}
    {{- fail "httproute.dnsWildcard (or ingress.dnsWildcard) must be set when using the 'edpDefault' hostname" }}
    {{- end }}
    - {{ $fullName }}-{{ $.Release.Namespace }}.{{ $dnsWildcard }}
    {{- else }}
    - {{ . | quote }}
    {{- end }}
    {{- end }}
  {{- end }}
  rules:
    - matches:
        {{- toYaml .Values.httproute.matches | nindent 8 }}
      backendRefs:
        - name: {{ $fullName }}
          port: {{ $svcPort | int }}
{{- end }}
