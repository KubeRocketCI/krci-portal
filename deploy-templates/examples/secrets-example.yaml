# Example with actual values (DO NOT USE IN PRODUCTION):
# apiVersion: v1
# kind: Secret
# metadata:
#   name: krci-portal
# type: Opaque
# stringData:  # Using stringData to avoid manual base64 encoding
#   oidcClientSecret: "your-client-secret"
---
# Example with actual values (DO NOT USE IN PRODUCTION):
# apiVersion: v1
# kind: Secret
# metadata:
#   name: krci-portal-server-secret
# type: Opaque
# stringData:  # Using stringData to avoid manual base64 encoding
#   serverSecret: "your-server-secret-key"
