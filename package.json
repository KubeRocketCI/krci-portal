{
  "name": "krci",
  "version": "0.6.0-SNAPSHOT",
  "description": "Monorepo KRCI Portal",
  "scripts": {
    "dev": "concurrently -k -n SHARED,TRPC,SERVER,CLIENT -c yellow,magenta,blue,green \"pnpm --filter=shared build:watch\" \"pnpm --filter=trpc build:watch\" \"pnpm --filter=server dev\" \"pnpm --filter=client dev\"",
    "build": "pnpm --filter=shared build && pnpm --filter=trpc build && pnpm --filter=server build && pnpm --filter=client build",
    "generate:openapi": "pnpm --filter=shared build && pnpm --filter=trpc build && pnpm --filter=trpc generate:openapi",
    "tsc:check": "pnpm --filter=shared tsc && pnpm --filter=trpc tsc && pnpm --filter=server tsc && pnpm --filter=client tsc",
    "lint:check": "pnpm --recursive run lint",
    "format:check": "pnpm --recursive run format:check",
    "format:fix": "pnpm --recursive run format",
    "test:coverage": "vitest run",
    "sonar:check-dev": "dotenv -e .env -- sh -c 'sonar-scanner -Dsonar.host.url=\"$SONAR_HOST_URL\" -Dsonar.token=\"$SONAR_TOKEN\" -X'",
    "prepare:validate": "pnpm tsc:check && pnpm lint:check && pnpm test:coverage && pnpm format:check",
    "clean:node_modules": "find . -name 'node_modules' -type d -prune -exec rm -rf {} +",
    "deploy:clean": "rimraf deploy",
    "deploy:prepare": "pnpm deploy --filter=client --prod deploy/client && pnpm deploy --filter=server --prod deploy/server",
    "deploy:post": "shx mkdir -p deploy/server/db && shx touch deploy/server/db/sessions.sqlite",
    "prepare:artifacts": "pnpm deploy:clean && pnpm deploy:prepare && pnpm deploy:post"
  },
  "packageManager": "pnpm@11.5.1+sha512.93f7b57422ea7068257235b4c16eb60762eb68e1dc23723199cc739043ea9be2c4143274a399d8c6defa2b1176226d9ca1c4b63482d6200c1a8fbaa78c1d1485",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@my-project/client": "workspace:^",
    "@my-project/server": "workspace:^",
    "@my-project/shared": "workspace:^",
    "concurrently": "^9.1.2",
    "immer": "^11.1.8",
    "monaco-yaml": "^5.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "@typescript-eslint/eslint-plugin": "^8.60.0",
    "@typescript-eslint/parser": "^8.32.1",
    "@vitest/coverage-istanbul": "^4.1.0",
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^8.0.0",
    "esbuild": "^0.25.4",
    "eslint": "^9.39.2",
    "prettier": "^3.5.3",
    "rimraf": "^6.1.0",
    "shx": "^0.4.0",
    "sonar-scanner": "^3.1.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.32.1",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^4.1.0"
  }
}
